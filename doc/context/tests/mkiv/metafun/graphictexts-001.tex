% for willi, reuse of graphic texts:

\startMPpage
    picture one   ; one   := image ( graphictext "1"
        withdrawcolor black
        withfillcolor white
        withpen pencircle scaled 2pt
    ) ysized 2cm ;
    picture two   ; two   := image ( graphictext "2"
        withdrawcolor red
        withfillcolor cyan
        withpen pencircle scaled 2pt
    ) ysized 2cm ;
    picture three ; three := image ( graphictext "3"
        withdrawcolor green
        withfillcolor magenta
        withpen pencircle scaled 2pt
    ) ysized 2cm ;
    picture four  ; four  := image ( graphictext "4"
        withdrawcolor blue
        withfillcolor yellow
        withpen pencircle scaled 2pt
    ) ysized 2cm ;

    draw one   shifted (0cm,0);
    draw one   shifted (2cm,0);
    draw two   shifted (4cm,0);
    draw two   shifted (6cm,0);
    draw three shifted (8cm,0);
    draw three shifted (10cm,0);
    draw four  shifted (12cm,0); % has artefact in lm
    draw four  shifted (14cm,0); % has artefact in lm
\stopMPpage
